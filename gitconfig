[user]
	name = Paulo Koch
	email = paulo.koch@gmail.com

[core]
	editor = vim

[alias]
	gl = log --graph --pretty=format:'%Cred%h%Creset - %s %Cgreen(%cr) <%an>' --abbrev-commit --date=relative
	b = branch
	st = status -sb
	ci = commit

[color]
	branch = auto
	diff = auto
	interactive = auto
	status = auto

[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green

[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold

[color "status"]
	added = yellow
	changed = green
	untracked = cyan

[github]
	user = pkoch
	token = 
